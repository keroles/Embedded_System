#include "stdio.h"

void main(){
	float x, y;
	printf("enter two number you want to swap \n");
	scanf("%f %f", &x, &y);
	printf("after swapping, the numbers are %f and %f \n", y, x);
	system("pause");
}